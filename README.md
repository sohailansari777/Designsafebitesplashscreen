
  # Design SafeBite Splash Screen

  This is a code bundle for Design SafeBite Splash Screen. The original project is available at https://www.figma.com/design/WMUY8QF9D5ImMHGuQg7dr6/Design-SafeBite-Splash-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  